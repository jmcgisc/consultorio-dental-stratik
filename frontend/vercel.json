{
  "version": 2,
  "projects": [
    {
      "src": "frontend",
      "use": "static",
      "buildCommand": "pnpm install && pnpm build",
      "outputDirectory": "dist",
      "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
    }
  ]
}
 